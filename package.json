{
  "name": "purg",
  "displayName": "Purg",
  "publisher": "purg",
  "description": "A lightweight code refurbisher and organizer.",
  "version": "1.0",
  "repository": "https://github.com/purgglobal/vscode_extension.git",
  "engines": {
    "vscode": "^1.102.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": ["onCommand:extension.purg"],
  "main": "./extension.js",
  "contributes": {
    "commands": [{
      "command": "purg.purg",
      "title": "purg"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.102.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "eslint": "^9.25.1",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  }
}
